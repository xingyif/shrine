//to create bindable properties we need to add the bindable decorator
//import {bindable} from 'aurelia-framework';
export class QueryTable {
    //@bindable queries;   // bind to queries, when ready.
}